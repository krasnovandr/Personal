@using Microsoft.Owin.Security
@*<div ng-controller="LoginController">
        <form name="answerForm">
            <fieldset>
                <p>
                    <label for="answerText">Логин</label>
                    <input id="answerText" ng-model="answer.UserName" required placeholder="Введите логин" />
                </p>
                <p>
                    <label for="answerAuthor">Пароль</label>
                    <input id="answerAuthor" ng-model="answer.Password" required placeholder="Введите пароль" />
                </p>
                <p>
                    <label for="answerDate">Запомнить</label>
                    <input type="checkbox" id="answerDate" ng-model="answer.RememberMe" required placeholder="Введите дату" />
                </p>
            </fieldset>
            <button type="submit" ng-click="save(answer, answerForm)">Сохранить</button>
        </form>
    </div>*@
<div ng-controller="LoginController" class="container-fluid">
    <div class="row">
        <div class="col-md-5 col-md-offset-3">
            <form ng-submit="login()" name="loginForm" novalidate>
                <div class="form-group">
                    <label for="userName">Логин:</label>
                    <input type="text" class="form-control" name="userName" ng-model="userName" id="userName" required />
                    <span style="color:red" ng-show="loginForm.userName.$dirty && loginForm.userName.$invalid">
                        <span ng-show="loginForm.userName.$error.required">Введите логин</span>
                    </span>
                </div>
                <div class="form-group">
                    <label for="password">Пароль:</label>
                    <input type="password" class="form-control" id="password" ng-model="password" />
                </div>
                <div class="form-group">
                    <label for="rememberMe">Запомнить меня:</label>
                    <input type="checkbox" id="rememberMe" ng-model="rememberMe" />
                </div>
                <div class="form-group">
                    <span style="color:red">{{loginError}}</span>
                </div>
                <button class="btn btn-default" type="submit"
                        ng-disabled="loginForm.userName.$dirty && loginForm.userName.$invalid">
                    Войти
                </button>
                <button class="btn btn-default" type="button" ng-show="errorMail" ng-click="repeatMail()"
                   >
                    Выслать письмо
                </button>
            </form>
        </div>
        @*<div class="col-md-2">
            <div ng-repeat="provider in providers">
                <div class="row">
                    <button class="btn-default" ng-click="externalAuthentification(provider)">{{provider.Caption}}</button>
                </div>
            </div>

        </div>*@
    </div>
</div>
@*<div ng-repeat
        </div>
    ="item in models.lists.A">
            {{item.label}}
        </div>*@



@*@{
        var loginProviders = Context.GetOwinContext().Authentication.GetExternalAuthenticationTypes();
        // string action = Model.Action;
        string returnUrl = " https://localhost:44300/signin-vkontakte";
        using (Html.BeginForm("ExternalLogin", "Account", new { ReturnUrl = returnUrl }))
        {
            @Html.AntiForgeryToken()
            <div id="socialLoginList">
                <p>
                    @foreach (AuthenticationDescription p in loginProviders)
                    {
                        <button type="submit" class="btn btn-default" id="@p.AuthenticationType" name="provider" value="@p.AuthenticationType" title="Log in using your @p.Caption account">@p.AuthenticationType</button>
                    }
                </p>
            </div>
        }

    }*@

@*public string UserName { get; set; }

    [Required]
    [DataType(DataType.Password)]
    [Display(Name = "Password")]
    public string Password { get; set; }

    [Display(Name = "Remember me?")]
    public bool RememberMe { get; set; }*@